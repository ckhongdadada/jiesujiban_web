# Deprecated Compatibility Layer

`enhancements/` is kept as a temporary compatibility shim for older scripts and notes.

Current production code should import from `src.jsjb.*` instead of `enhancements.*`.

The active runtime, training, web routes, tests, and scripts have already been migrated to the new package layout. This directory should not receive new business logic.
