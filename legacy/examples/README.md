# Legacy examples

These examples reference older experimental APIs or optimization modules that are no longer part of the main `src/jsjb` runtime path. They are archived for reference and should not be treated as runnable demos without updating their imports and assumptions.
